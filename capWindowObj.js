"use strict";
/**
 * # open() - carga un recurso en el contexto de un nuevo navegador o uno que ya existe
 * # close() - cierra la ventana actial, o la ventana en la que se llamo
 * # closed - indica si la ventana referenciada esta cerrada o no, devuelve true o false
 * # stop() - detiene la carga de recursos en el contexto de navegacion actual
 * # print() - abre el cuadro de dialogo imprimir para imprimir el documento actual
 * # confirm() -abre un cuadro de dialogo con un mensaje y dos botnes: aceptar y cancelar, esto retorna true o false
 */

// let url = "https://github.com/leon007k?tab=repositories"
// let open = window.open(url); 
// open.close(url);

/** 
* # screen - devielve una referencia al objeto de pantalla asociado con la ventana
* # screenLeft - devuelve la distancia horizontal entre el borde izquierdo del navegador y el borde izquierdo de la pantalla(solo lectura)
* # screenTop - devuelve la distancia vertical entre el borde de arriba del navegador y el borde de arriba de la pantalla(solo lectura)
* # scrollX e scrollY - devuelve el numero de pixeles que el documento se desplaza horizontalmente o verticalmente
* # scroll(), scrollTo() - desplaza la ventana a un lugar particular en el documento (con options y con posiciones)
 */

// const screen = window.screen;
// console.log(screen);
// document.write(screen.availWidth); 

/** 
 * # resizeBy() - cambia el tamaño de la ventana actual en una cantidad especifica
 * # resizeTo() - redimensiona dinamicamente la ventana
 * # window.location.href - Devuelve el href de la pagina actual
 * # window.location.hostname - devuelve el nombre del dominio del servidor web
 * # window.location.pastname - devuelve la ruta y el nombre del archivo de la pagina actual
 * # window.location.protocol - devuelve el protocolo web utilizado (http o https)
 * # window.location.assign() - carga un nuevo documento
*/
